<!-- Create variables with different primitive types
 (e.g., string, number, boolean, null, undefined, symbol)
  and log each variable’s type to the console.

Create an array and an object, then log their types to confirm they're reference types. -->